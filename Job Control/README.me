Program simulates a shell similar to bash with some functionalities such as which, exit, single command with option input and output redirection, multiple pipelined commands with optional input and output redirection.
